# ---------------------------------------------------------------------------
# CONFIDENTIAL — never publish
# ---------------------------------------------------------------------------
# reference/ holds the Green Belt courseware, the Green Belt WA (SAQ) and PP
# ASSESSMENT PAPERS AND ANSWER KEYS, and a copyrighted Six Sigma ebook.
# None of it may be published. NOTE the folder is spelled "asssesemnt" in the
# source, so a generic "assessment" filter does NOT catch it.
reference/

# Assessment papers and marking guides (any spelling) are trainer-only.
**/assessment/
# ...but DO version the generator itself. The papers and answer keys stay out of
# git; the code that builds them is reviewable and belongs in the repo.
!**/assessment/
!**/assessment/*.py
**/assessment/*.docx
**/assessment/*.pdf
**/assessments/
**/asssesemnt/
*Answers to *
*WA (SAQ)*
*PP Assessment*

# ---------------------------------------------------------------------------
# Trainer-only lab answer keys
# ---------------------------------------------------------------------------
# trainer-notes/ holds the expected finding for every lab dataset. Learners who
# read it first do not do the lab, so it stays out of the public repository.
# The GENERATOR that writes it lives in .claude/ and is versioned.
trainer-notes/

# ---------------------------------------------------------------------------
# Oversized source material (GitHub hard-rejects files over 100 MB)
# ---------------------------------------------------------------------------
# The original 113 MB v21 trainer deck is the input to the rebuild, not an
# output of it. Keep it locally; it does not belong in the repo.
WSQ -*v21.pptx
*.zip

# ---------------------------------------------------------------------------
# Secrets
# ---------------------------------------------------------------------------
.env
.env.*
*credentials*.json
*token*.json
*client_secret*.json

# ---------------------------------------------------------------------------
# OS / office junk
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
~$*
.~lock.*#
*.tmp

# Python bytecode from the build pipeline
__pycache__/
*.pyc

# Superseded lab folders from the old slug-named layout. Kept on disk as an
# archive (never deleted), but not republished — the live packs are labs/"NN - Title"/.
labs/archive/

# ---------------------------------------------------------------------------
# Private build tooling and superseded versions
# ---------------------------------------------------------------------------
# The private courseware build pipeline (skills, agents, hooks). Trainer tooling,
# not learner courseware. Kept on disk — it is the single source every artifact
# is generated from — but not part of the public package.
.claude/

# Superseded deck versions. The public package carries the CURRENT version only;
# the archive is retained locally so nothing is ever lost.
courseware/archive/
